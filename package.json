{
  "name": "specloom-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "example:solidjs:install": "pnpm --dir examples/solid-project install",
    "example:solidjs:dev": "pnpm --dir examples/solid-project dev",
    "example:solidjs:build": "pnpm --dir examples/solid-project build"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.0.17",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "vitest": "4.0.17"
  }
}
